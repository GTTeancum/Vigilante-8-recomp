# Bounded four-player performance assessment � 2026-09-10

No performance implementation changes or new goal. Tested staged SHA256 5F2AC88A778CFA909AF67120403C9FA447DDDE80F72EDF9BB56050CE1A2A1766. Existing dirty work preserved.

Two sequential, hidden, process-local Bayou runs, four synthetic players and two AI, 700 polls, Enhanced preset, captures disabled. Profile: 57.30 seconds, 13.37 median / 10.22 minimum FPS. Control with Enhanced performance tracing off: 60.49 seconds, 12.82 median / 10.76 minimum. Both exited zero and verified six actors. Variability precludes interpreting their difference as instrumentation overhead or an improvement. Prior 13.84/12.30 baseline remains a different run.

Existing detailed counters, averaged across eight complete 60-frame windows beginning at frame121: 77.583ms frame interval; 14.439ms DrawTri inclusive CPU time; 3.569ms flush inclusive CPU time; 2.520ms texture resolve time; 0.041ms backend Present time; 19.720MiB main-thread allocations/frame; 959.717 flushes/frame; 11876 DrawTri calls/frame; 17158 generated terrain triangles/frame; 35887 recorded widescreen projection vertices/frame; 15828 texture resolves/frame; 8 VRAM readbacks/frame; 5 MSAA resolves/frame. Nested timers overlap; do not sum them or interpret host API timings as GPU execution durations. Present does not cover the whole host swap/event loop. Allocation sources and GC pause duration were not sampled.

Rank by potential payoff (not a measured promise of recovered FPS):
1. Pre-submission work / translated rendering preparation: largest unaccounted frame-time region. Four world traversals are visible in SplitHarnessRender. Direct function timing is needed before assigning this residual to simulation, projection, packet processing, memory bookkeeping or readback stalls. High potential, medium confidence in specific targets.
2. Temporary allocation reuse: ~19.7MiB/frame and repeated collections. Reusable per-view/deferred buffers are candidate local improvements. BufferedDeferredBatch creates lists and discards batches; this is a source candidate, not proven allocation attribution. High potential, exact gain unknown. Best bounded next investigation: allocation attribution, then optimize the largest proven producer only.
3. Preserve semantics while avoiding repeated GPU-to-CPU VRAM feedback: eight actual ReadRect calls per frame; these can synchronize GPU/CPU. ReadVram retains framebuffer writeback boundaries; must preserve exact feedback and cache invalidation. Medium/high potential but no measured stall duration.
4. Reduce compatible batch/state changes: ~960 flushes/frame, dominated by depth-state transitions in logs. ~3.6ms direct inclusive CPU flush cost suggests a modest direct gain, not enough by itself for 40FPS. Additional GPU effects unmeasured. Preserve transparency and depth ordering.
5. Texture lookup/cache overhead: ~2.5ms/frame with high cache hit rate. Smaller direct opportunity; do not begin here.

Earlier DrawSync speculation was incorrect: LibGpu.DrawSync simply sets V0=0. Separate view buffers do not remove a blocking GPU wait at that call because none exists. No GL Finish call was found in this backend. HUD/presentation simplification is not supported as a major next target by these measurements. Dark P4 water is not a defect.

Evidence: artifacts/split-screen-assessment-20260910/{profile,control}/runtime.log and result.json; metrics.json contains normalized detailed counters. Assessment ends here; no optimization implemented.

## Increment 1: water upload allocation (2026-09-10)
User authorized shared PC/Xbox efficiency work, incrementally. A 20-second EventPipe allocation sample identified GlVertex[] growth in ReplaceWithDreamcastWaterSurfaceMesh as the largest allocation stack (~3867MiB sampled allocation weight); next stacks were terrain distance-color samples and projected water vertices. Sampling totals are allocation weights, not retained memory.

Implemented one change only: convert the existing water surface vertices directly into the existing upload array after reserving its destination range. Removed the per-submission list growth and list-to-upload copy; retained conversion math, vertex count/order, material and depth state. No additional retained buffer. This avoids work in both a managed runtime and a future native port.

Eight corresponding detailed 60-frame windows: main-thread allocation19.720 ->6.404MiB/frame (67.5% reduction). Matching earlier profile13.37 median/10.22 minimum FPS; after13.83/11.85. Single-run differences are small and variable, not proof of a substantial FPS increase. Extra pre-change EventPipe run13.31/11.23 is instrumented differently. Do not use visual-before FPS: build activity overlapped that visual run.

Release publish passed. Staged SHA2568D08462F4D7457EFFA94BA4FFDF02F70B1A63F66B2ACBD3706FC17816BFB066E. Native after benchmark and equipped visual run exited0 with exactly six actors. Before/after captures120 and350 visually inspected: terrain, water and HUD remain present; images are not pixel-identical and show animation/effect differences. This is bounded Bayou visual coverage, not all-level or all-frame acceptance. Evidence artifacts/split-allocation-20260910 includes allocation trace/stacks, original source snapshot, benchmark/capture logs and comparison.json. No commit/push. Stop after this increment.

## Increment 2: batch edge-repair readback (2026-09-10)
User requested the next bounded increment with emphasis on FPS. Sampled thread-time trace (dotnet-sampled-thread-time; first cpu-sampling profile request was invalid on Windows) implicated GlVram.ReadRect through ProcessExpandedEdgePool. Of419 rendering-stack samples,120 were in ReadRect and111 in GC polling; these are sampled wall-clock stacks, not GPU timings and not an allocation attribution.

Changed shared ProcessExpandedEdgePool to read a bounding rectangle of all requested pixels once and reuse that CPU buffer. Native repair packet generation and white-pixel decisions remain unchanged. Batched mode defaults on when HLE backend is ready; RECOMPONE_V82_BATCH_EDGE_READBACK=0 restores original path. RECOMPONE_V82_VERIFY_EDGE_READBACK=1 compares each 16-bit pixel with the original native StoreImage path and throws on any mismatch. Rectangles retain native1024x512 wrap coordinates; worst-case reusable buffer bounded by full VRAM1MiB, observed2134bytes. The pass builds packets but submits them later, so probes read one stable image.

Release build passed; staged592B039102740BB3CAE6672449B8FACEFB3E1DF939B6415F7B651172403C5CD9. Four-player500-poll equipped verification exited0, with8268 exact matches logged by tick360 and no mismatches through completion. Inspected capture350, water/terrain/HUD intact. Short two-player160-poll compatibility run exited0 with six actors; no pixel-verification count logged in that short run, so it is not claimed as additional oracle coverage.

Readbacks8->4/frame; MSAA resolves5->1/frame; recorded download pixels32768->4268/frame in steady windows. However, before13.70 median/11.85 minimum FPS versus after13.64/11.72: NO measurable FPS improvement. Before had sampled-thread tracing late in the run; after did not. Earlier increment1 after13.83 is likewise within this variation. This is a verified reduction in transfers/work, not a demonstrated speedup or evidence of an Xbox FPS gain. Retained as shared efficiency work, no commit/push; stop here. Further priority must be based on narrower frame/GC/CPU timing rather than assuming these readback stalls accounted for independently removable frame time.

Evidence artifacts/split-cpu-20260910: source-before snapshot, trace/stacks, before/after results, verify and verify-2p logs, proof.png. No graphics settings/fidelity changes.

## FPS-only pass: rejected candidates (2026-09-10)
User refocused acceptance on FPS and explicitly prioritized runtime over loading time. Temporary stage instrumentation separated setup, native world preparation, OT submission, repair readback and HUD; GetTotalPauseDuration measured GC pauses. Mature cache-off windows averaged75.73ms interval,29.52ms world,25.64ms submit,17.24ms repair,0.54ms GC pause. GC pause is nested, not additive. These support targeting repeated rendering preparation/submission; the previous sampled PollGC stacks did not justify calling GC the primary bottleneck.

Rejected tests: disabling tiered compilation was slower9.42median; per-traversal corner/material dictionary cache13.77off ->14.17on, world29.52->27.18ms but only a small noisy FPS difference; fixed-size direct cache13.60median; CPU register provenance bitmap13.03median. Terrain oracle logged17,260,876 exact comparisons without mismatch; CPU oracle passed50,000 randomized original-vs-new register, provenance, store, snapshot and restore steps. Correctness did not establish a useful FPS improvement. No candidate retained. Stage instrumentation removed from production source, archived under artifacts/split-fps-20260910 along with rejected candidates and logs. Original runtime and harness files restored; V82Compat retains only pre-pass edits. Previous staged executable592B0391 restored. No commit/push or new goal.

User accepts longer loading if it improves gameplay. Next architectural investigation should consider load-time preparation of reusable model/terrain data and reduction of repeated per-camera work, with explicit steady-state FPS acceptance; do not claim such a cache is already implemented or safe without handling mutable data.

## Accepted architectural increment and counter correction

See `v82_direct_scene_performance.md` for the final repeated comparison: 14.66–15.09 median FPS control versus 24.93–25.89 optimized, with approximately 28 ms/frame saved across matched windows. This supersedes the earlier candidate rankings.

Correction to the earlier MSAA statements: the reported 5-to-1 counter change omitted direct ResolveRegion calls through WritebackRegion and must not be interpreted as total actual resolves falling from five to one. The readback increment had no demonstrated FPS gain. DrawSync is a no-op, and measured GC pause duration did not support GC as the dominant bottleneck. The accepted gain comes from bypassing redundant terrain packet production, sharing validated source data and conservative visibility rejection.
